function click1() {
    let n1 = Math.trunc(Number(window.prompt('Digite um número')))
    window.alert(`Antes de ${n1} temos o número ${Math.trunc(n1-1)}. Depois de ${n1} temos o número ${Math.trunc(n1+1)}`)
}

